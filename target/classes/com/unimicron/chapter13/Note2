對象匹配

對象匹配，甚麼才算是匹配呢?規則如下:
1. case中，對象的unapply()(該方法為對象提取器)返回Some集合，則為匹配成功
2. 返回None，則為匹配失敗

注意事項
1. 當case後面的對象提取器方法的參數為多個時，默認調用def unapplySeq()
2. 如果unapplySeq()返回的是Some，獲取其中的值，接著判斷得到的sequence中的元素個數是否與case中定義的對象提取器的參
   數個數一致，如果一致，則把這些元素分別取出，賦值給對象提取器的參數
3. 其他規則不變

變量聲明中的模式
match中每一個case都可以單獨提取出來，意思是一樣的