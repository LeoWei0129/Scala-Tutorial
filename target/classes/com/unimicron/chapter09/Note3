富接口(當作富接口使用的特質)
即該特質中既有抽象方法，又有非抽象方法
(EX):
trait Operate{
    def insert() // 抽象
    def delete(): Unit = { // 實現
        println("None")
    }
}

特質中的具體字段
特質中可以定義具體的字段，如果初始化了就是具體字段，如果不初始化就是抽象字段，混入該特質的類就具有了該字段，字段不是繼承，而是直接加入
類，成為自己的字段
父特質的普通字段，被直接加到混入對象中

特質中的抽象字段
特質中未被初始化的字段在具體的子類中必須被重寫(透過override，如同匿名子類實現抽象方法的方式，不能直接賦值)